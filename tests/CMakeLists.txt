find_package(Qt6 REQUIRED COMPONENTS Test)

add_subdirectory(tst_net)
add_subdirectory(tst_peercast)
add_subdirectory(tst_watchdog)
add_subdirectory(tst_bbs)
add_subdirectory(tst_display)     # M4.0: 窓サイズ純計算テスト
